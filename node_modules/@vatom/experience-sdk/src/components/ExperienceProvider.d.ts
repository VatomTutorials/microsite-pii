import React, { PropsWithChildren } from 'react';
import { ExperienceTypes } from '../types';
declare global {
    interface Window {
        ReactNativeWebView: any;
    }
}
export declare const ExperienceContext: React.Context<ExperienceTypes>;
export declare const useExperience: () => ExperienceTypes;
export declare const ExperienceProvider: (props: PropsWithChildren) => JSX.Element;
