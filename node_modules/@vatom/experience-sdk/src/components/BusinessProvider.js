import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useState } from 'react';
import { useMessage } from '../hooks/useMessage';
export const BusinessConext = createContext({});
export const BusinessProvider = (props) => {
    const [business, setBusiness] = useState();
    const [tokens, setTokens] = useState([]);
    useMessage('GET_BUSINESS', (_, payload) => {
        if (payload) {
            setBusiness(payload);
        }
    });
    useMessage('GET_BUSINESS_TOKENS', (_, payload) => {
        if (payload) {
            setTokens(payload);
        }
    });
    useMessage('SITE_GO_BACK', () => {
        window.history.back();
    });
    return (_jsx(BusinessConext.Provider, { value: { business, tokens }, children: props.children }));
};
//# sourceMappingURL=BusinessProvider.js.map