import React, { PropsWithChildren } from 'react';
import { UserProviderTypes } from '../types';
export declare const UserContext: React.Context<UserProviderTypes<unknown, unknown>>;
export declare const UserProvider: (props: PropsWithChildren) => JSX.Element;
